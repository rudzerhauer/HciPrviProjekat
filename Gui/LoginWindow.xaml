<Window x:Class="WpfApp1.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Coffee Shop Login" Height="450" Width="800">
    <!-- Your content here -->
    <Window.Background>
       <ImageBrush ImageSource="images/coffee_shop_background.jpg" Stretch="UniformToFill" />
    </Window.Background>
    <Grid>
        <!-- Semi-Transparent Form with CornerRadius -->
        <Border Background="#CCFFFFFF" CornerRadius="15" Width="400" Height="300" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Title -->
                <TextBlock Text="Welcome to Coffee Bliss" FontSize="20" FontWeight="Bold" 
                           Foreground="#8B4513" HorizontalAlignment="Center" Margin="0,10" />

                <!-- Username Field with Placeholder -->
                <Grid Grid.Row="1" Margin="0,20,0,0" VerticalAlignment="Center">
                    <TextBox x:Name="UsernameTextBox" FontSize="14" 
                             BorderBrush="#8B4513" BorderThickness="2" Height="40" />
                    <TextBlock Text="Username" Foreground="Gray" 
                               Margin="5,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=UsernameTextBox}" Value="">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Text, ElementName=UsernameTextBox}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Text, ElementName=UsernameTextBox}" Value="{x:Static sys:String.Empty}">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                    <Trigger Property="Visibility" Value="Collapsed" />
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

                <!-- Password Field with Placeholder -->
                <Grid Grid.Row="2" Margin="0,10,0,0" VerticalAlignment="Center">
                    <PasswordBox x:Name="PasswordBox" FontSize="14" 
                                 BorderBrush="#8B4513" BorderThickness="2" Height="40" />
                    <TextBlock Text="Password" Foreground="Gray" 
                               Margin="5,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Password, ElementName=PasswordBox}" Value="">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                    <Trigger Property="Visibility" Value="Collapsed" />
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

                <!-- Login Button -->
                <Button Content="Login" Grid.Row="3" Height="40" Margin="0,20,0,0" 
        Background="#8B4513" Foreground="White" FontSize="16" 
        FontWeight="Bold" Cursor="Hand" BorderBrush="#8B4513" BorderThickness="0" 
        Click="LoginButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="20">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

            </Grid>
        </Border>
    </Grid>
</Window>
